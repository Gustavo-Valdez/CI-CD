pipeline {
    agent any 

    stages {
        stage('install') {
            steps {
                sh 'pwd'
                dir('/var/www/html') {
                    sh 'pwd'
                }
            }
        }
    }
}